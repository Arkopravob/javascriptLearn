// const inputs = [2, 3, 4, 7, 9];

// const sqauredArray = inputs.map(function (
//     currentElement
// ) {
//     return currentElement * currentElement;
// });

// const sqauredArray = inputs.map((nums) => nums * nums);

//cube of number;
// const cubedArray = inputs.map((nums

// ) => nums * nums * nums)
// console.log(cubedArray);

//extracting names
const teamMember = [
    { id: 1, name: "ARko" },
    { id: 2, name: "Virat" },
    { id: 3, name: "rohit" }
];
const names = teamMember.map((member) => member.name);
console.log(names);

